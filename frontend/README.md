# Frontend

Next.js operator console for AI Reel Factory.

```bash
npm install
npm run dev
```

The frontend calls the FastAPI backend through:

```text
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
